# Building containers

Put the standalone installer tarball for Aria Config in this directory. You get this from [My VMware](https://customerconnect.vmware.com/home).

Then run `./build-containers.sh --help` to build raas, master, and minion containers.
